#pragma once
/*
#include <glad/gl.h>
#include <GLFW/glfw3.h>
#include <string>

namespace GFX {

class Texture {
private:
    GLuint texture;
    unsigned char *data;

public:
    GLuint getTexture();
    void bind();

    Texture(std::string texturePath);
    ~Texture();
};

}; // namespace GFX
*/
